import { Metadata } from "next";

export const metadata: Metadata = {
    title: "OCR PDF to Text Free - Convert Scanned PDFs to Editable Text | SendNow",
    description: "Convert scanned PDFs to editable text using OCR. Extract text from images and scanned documents. Free online OCR tool, no signup required.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
