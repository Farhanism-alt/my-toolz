import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Chat with PDF Free - Ask Questions & Get Instant Answers | SendNow",
    description: "Upload any PDF and chat with it using AI. Ask questions, get summaries, extract insights instantly. Free, no signup required. Try it now.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
