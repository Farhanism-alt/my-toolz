import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Chat with Word Documents Free - AI-Powered DOCX Q&A | SendNow",
    description: "Upload Word documents and ask questions. Get instant summaries, extract information, analyze content with AI. Free DOCX chat tool, no signup needed.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
