import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Split Word Document Online Free - Divide DOCX File | SendNow",
    description: "Extract chapters or sections from your Word documents. Split DOCX and DOC files into smaller files easily. Free online tool.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
