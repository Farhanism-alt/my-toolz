import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Chat with PowerPoint Free - Analyze Presentations with AI | SendNow",
    description: "Upload PowerPoint presentations and ask questions. Extract slide content, summarize decks, analyze presentations with AI. Free PPTX chat tool.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
