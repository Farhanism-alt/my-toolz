import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Chat with Video Free - Get Transcripts & Summaries with AI | SendNow",
    description: "Upload videos and chat with them. Get instant transcripts, summaries, and answers to questions about video content. Free AI video analysis tool.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
