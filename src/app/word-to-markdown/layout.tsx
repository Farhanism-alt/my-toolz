import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Convert Word to Markdown Free - DOCX to MD Online | SendNow",
    description: "Convert Word documents (.docx) to clean Markdown (.md) for free. Perfect for developers and documentation writers.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
