import { Metadata } from "next";

export const metadata: Metadata = {
    title: "PDF to Text Converter Free - Extract Text from PDF Online | SendNow",
    description: "Convert PDF to text instantly. Extract all text from PDF files online, free. No signup, no limits. Copy or download extracted text immediately.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
