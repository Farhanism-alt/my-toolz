import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Convert PDF to PNG Free - PDF to Image Converter | SendNow",
    description: "Convert your PDF pages to high-quality PNG images instantly. Free online PDF to image converter with no registration required.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
