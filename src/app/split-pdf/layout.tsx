import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Split PDF Online Free - Divide PDF Pages | SendNow",
    description: "Split your PDF into multiple documents or extract specific pages for free. Safe and secure online PDF page extraction.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
