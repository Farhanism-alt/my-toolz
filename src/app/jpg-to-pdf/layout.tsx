import { Metadata } from "next";

export const metadata: Metadata = {
    title: "JPG to PDF Converter Free - Image to PDF Online | SendNow",
    description: "Convert your JPG/JPEG photos to PDF documents instantly. Merge multiple images into one PDF file for free.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
