import { Metadata } from "next";

export const metadata: Metadata = {
    title: "PDF to JPG Converter Free - Convert PDF to Image Online | SendNow",
    description: "Convert PDF pages to high-quality JPG images for free. Fast online PDF to JPEG converter. No registration or software required.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
